export const metadata = {
  title: 'Scholarship Vault | IvyPath',
  description: 'Discover and secure funding to support your pursuit of academic excellence.',
};

export default function ScholarshipsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
